# Contributing

Install dependencies with `npm ci`. Before opening a pull request, run:

```sh
npm test
npm run build
npm run build:demo
```

Use Conventional Commit messages so Release Please can prepare version updates and release notes.
